<Window x:Class="FocusGuard.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FocusGuard"
        Icon="Resources/app.ico"
        Width="400" Height="300"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        UseLayoutRounding="True">

    <Border x:Name="Root"
            CornerRadius="16"
            Background="#FAFAFA"
            BorderBrush="#E5E7EB"
            BorderThickness="1"
            MouseLeftButtonDown="Root_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="20,14,12,0">
                <Button x:Name="CloseButton"
                        DockPanel.Dock="Right"
                        Content="✕"
                        Width="26" Height="26"
                        FontSize="13"
                        Foreground="#B4B2A9"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        FocusVisualStyle="{x:Null}"
                        Click="CloseButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="Transparent">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}" />
                            </Border>
                            <!-- 自定义模板会整体替换默认模板，禁用态不会自动变灰，必须自己给 -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- 最小化键画一条横线，而不是用字体里的字形：微软雅黑没有 ✕(U+2715)，
                     ✕ 是靠回退到 Segoe UI Symbol 显示的（13px 下墨迹 10x9）；
                     而雅黑自带的 −(U+2212) 只有 8x1，笔画细一半，看着明显小一圈。
                     字体里没有能匹配 ✕ 的横线字形（U+2014 有 15 宽、太宽），所以直接画。 -->
                <Button x:Name="MinimizeButton"
                        DockPanel.Dock="Right"
                        Width="26" Height="26"
                        Foreground="#B4B2A9"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        FocusVisualStyle="{x:Null}"
                        Click="MinimizeButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="Transparent">
                                <Border Width="10" Height="2"
                                        HorizontalAlignment="Center" VerticalAlignment="Center"
                                        Background="{TemplateBinding Foreground}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <TextBlock x:Name="TitleText"
                           Text="FocusGuard"
                           FontSize="12"
                           Foreground="#9CA3AF"
                           VerticalAlignment="Center" />
            </DockPanel>

            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button x:Name="ActionButton"
                        Content="开始专注"
                        Width="248" Height="76"
                        FontSize="19"
                        Cursor="Hand"
                        FocusVisualStyle="{x:Null}"
                        Background="#185FA5"
                        Foreground="White"
                        Click="ActionButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd"
                                    CornerRadius="16"
                                    Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.86" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <TextBlock x:Name="ElapsedText"
                           Text=""
                           FontSize="12"
                           Foreground="#9CA3AF"
                           Margin="0,16,0,0"
                           HorizontalAlignment="Center" />
            </StackPanel>

            <TextBlock x:Name="HintText"
                       Grid.Row="2"
                       Text=""
                       FontSize="11"
                       Foreground="#B4B2A9"
                       Margin="24,0,24,18"
                       TextAlignment="Center"
                       TextWrapping="Wrap" />
        </Grid>
    </Border>
</Window>
